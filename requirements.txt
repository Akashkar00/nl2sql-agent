# ============================================================
# LOCAL DEV — install everything: pip install -r requirements.txt
# KAGGLE   — DON'T use this file. See notebooks/kaggle_train.ipynb Cell 2.
#             Kaggle pre-installs torch/transformers/etc. Installing over
#             them breaks the environment.
# ============================================================

# Core training stack
torch>=2.3.0
transformers>=4.51.3
trl>=0.18.2
peft>=0.18.0
accelerate>=0.34.1
bitsandbytes>=0.43.3
datasets>=3.4.1

# Unsloth (install separately: pip install unsloth)
# unsloth deps
cut_cross_entropy
hf_transfer
msgspec

# Retrieval
sentence-transformers>=3.0.1
faiss-cpu>=1.8.0

# DB execution
sqlalchemy>=2.0.31
sqlparse>=0.5.0

# Serving + demo
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
streamlit>=1.36.0
pydantic>=2.8.2

# Tracking + utilities
wandb>=0.17.5
huggingface_hub>=0.34.0
python-dotenv>=1.0.1
pyyaml>=6.0.1
tqdm>=4.66.4

# Dev
pytest>=8.2.2
ruff>=0.5.5
